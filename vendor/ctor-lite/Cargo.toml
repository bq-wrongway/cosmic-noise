# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
rust-version = "1.36.0"
name = "ctor-lite"
version = "0.1.0"
authors = ["John Nunley <dev@notgull.net>"]
description = "Run code at program startup or shutdown"
homepage = "https://codeberg.org/notgull/ctor-lite"
readme = "README.md"
keywords = [
    "ctor",
    "macros",
]
categories = ["macros"]
license = "MIT OR Apache-2.0"
repository = "https://codeberg.org/notgull/ctor-lite"

[dev-dependencies.macro_rules_attribute]
version = "0.2.0"

[dev-dependencies.rustix]
version = "0.38.34"
features = ["stdio"]
default-features = false

[features]
alloc = []
default = ["std"]
std = ["alloc"]
